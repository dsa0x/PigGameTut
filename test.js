var roundScore, prevscore, activePlayer, scores

prevscore = 0

var dice = Math.floor(Math.random() * 6) + 1
if (dice > 1) {
    roundScore += dice
   // if (prevscore !== roundScore && prevscore !== 6) {
        
   // }
}

console.log(prevscore)
console.log(roundScore)